diff --git a/CMakeLists.txt b/CMakeLists.txt
index e95068d..bec8086 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -6,6 +6,10 @@ set (CMAKE_INSTALL_PREFIX ${CMAKE_CURRENT_BINARY_DIR})
 set (CMAKE_INSTALL_LIBDIR ${CMAKE_INSTALL_PREFIX}/nlopt)
 set (INSTALL_PYTHON_DIR ${CMAKE_INSTALL_PREFIX})
 
+message(-------------------------------CMAKE_INSTALL_PREFIX = ${CMAKE_INSTALL_PREFIX})
+message(-------------------------------CMAKE_INSTALL_LIBDIR = ${CMAKE_INSTALL_LIBDIR})
+message(-------------------------------INSTALL_PYTHON_DIR = ${INSTALL_PYTHON_DIR})
+
 option (BUILD_SHARED_LIBS OFF)
 
 if (NOT WIN32)
diff --git a/setup.py b/setup.py
index c1d96ca..2e399cd 100644
--- a/setup.py
+++ b/setup.py
@@ -18,7 +18,8 @@ with open(Path(__file__).parent / "extern" / "nlopt" / "CMakeLists.txt") as f:
 
 
 setup(
-    version=version,
+    # version=version,
+    version="2.7.1",
     install_requires=["numpy >=1.18.5"],
     ext_modules=[NLOptBuildExtension("nlopt._nlopt", version)],
     cmdclass={"build_ext": NLOptBuild},
